def main() {
  var x = 10 : int;
  print(x);                 // 10
  var y = x + 20 : int;
  print(y);                 // 30
  var z = y * 42 : int;
  print(z);                 // 1260
  var w = x * y - z : int;
}