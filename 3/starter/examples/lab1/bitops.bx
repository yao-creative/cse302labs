def main() {
  var x = 0 : int;
  var y = 0 : int;
  var z = 0 : int;

  x = 10;
  y = 42;
  z = -100;
  print(x & y);   // 10
  print(x & z);   // 8
  print(x | y);   // 42
  print(x | z);   // -98
  print(x ^ y);   // 32
  print(x ^ z);   // -106
  print(~x);      // -11
  print(~z);      // 99
}