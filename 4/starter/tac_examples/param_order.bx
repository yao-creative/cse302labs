// fizz buzz
// print numbers from 0 to 100,
//   but replace every multiple of 3 with 333
//           and every multiple of 5 with 555
//   except replace multiples of both 3 and 5 with 151515

def fizzbuzz(f, s, t : int, fr, fi : int) {
    print(f);
    print(s);
    print(t);
    print(fr);
    print(fi);
}

def main() {
  fizzbuzz(1, 2, 3, 4, 5);
}